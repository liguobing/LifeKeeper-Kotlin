<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp">

    <EditText
        android:id="@+id/et_account_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:hint="@string/add_account_dialog_input_new_account_name"
        android:inputType="text"
        android:theme="@style/BlueBottomLineEditText" />

    <Button
        android:id="@+id/bt_submit_add"
        style="?android:attr/buttonBarButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/et_account_name"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="5dp"
        android:background="@drawable/bill_account___add_account_dialog___button_background"
        android:paddingStart="50dp"
        android:paddingEnd="50dp"
        android:text="@string/add_account_dialog_submit_add"
        android:textColor="@color/black" />
</RelativeLayout>